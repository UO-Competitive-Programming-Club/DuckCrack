#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

/*
 * char middleOperator;
   if (middleOperator == '&') {
        T[i][j] += T[i][k] * T[k + 1][j];
   } else if (middleOperator == '|') {
        T[i][j] += (F[i][k] * T[k + 1][j]) 
                +  (T[i][k] * F[k + 1][j])
                +  (T[i][k] * T[k + 1][j]);
   } else if (middleOperator == '^') {
        T[i][j] += (F[i][k] * T[k + 1][j]) 
                +  (T[i][k] * F[k + 1][j]);
   }
 * */
 
int CountParenthesization(vector<char> symbols, vector<char> operators) {


}
