#include "../Debug.h"
using namespace std;

/* this problems is trival if we can use additional space */
void Rotate2DArray(vector<vector<int>>& matrix) {
    int layer_nums = matrix.size() / 2,
        mlen = matrix.size();

    for (int cur_layer_idx = 0; cur_layer_idx < layer_nums; ++cur_layer_idx) {
        for (int i = 0; i < mlen; ++i) {
            
        }
    }
}
