#include "../Debug.h"

using namespace std; 

int RemoveDuplicates(vector<int>& A) {

}

int main(void) {
                            /* w */  
    vector<int> A = { 1, 1, 1, 1, 1, 2, 2, 2, 3, 3, 4, 4, 5, 6, 7, 8};
                         /* i */
    RemoveDuplicates(A);
    print_vector(A);

    return 0;
}
