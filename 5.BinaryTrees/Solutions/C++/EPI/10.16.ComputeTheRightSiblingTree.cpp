#include "../Debug.h"
using namespace std;

struct TreeLinkNode {
	TreeLinkNode *left;
	TreeLinkNode *right;
	TreeLinkNode *next;
};

void connect(TreeLinkNode *root) {
    auto iter = root;
    while (iter) {
        auto level_iter = iter;

    }
}


